apiVersion: v1
kind: Service
metadata:
  name: devops-training-service
spec:
  type: NodePort
  selector:
    app: devops-training-app
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007